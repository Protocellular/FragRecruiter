

To generate plots, there are two options.

1. Run the executable fragment_recruit like this:

./fragment_recruit -r ref_genome.fna -a alignment.sam


2. If that doesn't work, then you can also run it as:

python3 fragment_recruit.py -r ref_genome.fna -a alignment.sam



